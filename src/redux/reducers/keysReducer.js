import {UPDATE_KEYS_LIST} from '../actions/actionTypes'

export const updateKeysList = (state = {}, action) => {
    switch (action.type) {
        case UPDATE_KEYS_LIST: {
            return (state);
        }
        default: {
            return (state);
        }
    }
}